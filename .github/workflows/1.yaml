name: sample one yaml
on:
  push:
    branches:
      - "main"
jobs:
  job1:
    on: ["self-host"]
    steps:
      - name: code-checkout
        uses: actions/checkout@v4
      - name: print
        run: |
          printenv
      - name: list
        run: |
          ls -la